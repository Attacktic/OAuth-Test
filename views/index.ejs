<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>login test</title>
  </head>
  <body>
    welcome to login test
    <% if (logout === true){ %>
      <button type="button" name="button" onclick="location.href = '/logout';">Logout</button>
    <% } else { %>
      <button type="button" name="button" onclick="location.href = '/signin';">Sign in</button>
      <button type="button" name="button" onclick="location.href = '/signup';">Sign Up</button>
    <% } %>
  </body>
</html>
